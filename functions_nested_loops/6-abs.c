#include "main.h"
#include <stdio.h>
/**
 * _abs - function that computes the absolute value of an integer
 * @r: the character used to be checked
 * Return: r && -r
 */

int _abs(int r)
{
	if (r >= 0)
	return (r);
	else
	return (-r);
}
