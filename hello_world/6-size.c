#include <stdio.h>

/**
 * main - prints the size of various types on the computer it is compiled
 * Return:0
 */

int main(void)

	printf(char);
	printf(int);
	printf(long int);
	printf(long long int);
	printf(float);

	return (0);
}
